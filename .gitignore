# Run output directories and raw/processed data (root-anchored: a leading slash
# keeps these to the repo root so they do NOT also match the source package
# tessera/data/, which must be tracked).
# (Stooq_csv/ holds the raw downloaded source CSVs, kept local like data/)
/runs/
/data/
/Stooq_csv/

# Python
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# OS / editor
.DS_Store
